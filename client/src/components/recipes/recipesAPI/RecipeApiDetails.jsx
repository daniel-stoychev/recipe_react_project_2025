import { useEffect, useState } from "react";

export default function RecipeDetails() {
  const [recipeDetals, setRecipeDetails] = useState([]);

  useEffect(() => {
    async function loadRecipeDara(recipeId) {}
  });

  return (
    <>
      <h1>Details page</h1>
    </>
  );
}
